import express from 'express';
// import { authenticate, authorize } from '../middleware/auth.middleware';
// import * as analyticsController from '../controllers/analytics.controller';

const router = express.Router();

// TODO: Implement analytics routes (typically manager only)
// router.get('/sales', authenticate, authorize('MANAGER'), analyticsController.getSalesAnalytics);
// router.get('/products', authenticate, authorize('MANAGER'), analyticsController.getProductAnalytics);
// router.get('/employees', authenticate, authorize('MANAGER'), analyticsController.getEmployeeAnalytics);

export default router;

